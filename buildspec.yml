version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: latest
    commands:
      - npm ci
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - echo "✅ Build completed"
artifacts:
  base-directory: .
  files:
    - '.next/**'
    - 'public/**'
    - 'package.json'
    - 'package-lock.json'
    - 'next.config.js'
    - '!**/*.md'
    - '!**/docs/**'
    - '!**/diagrams/**'
    - '!node_modules/**'
